# 0S 三格真页移植计划（阶段 B6：保养 / 功能障碍 / 凯格尔）

> 状态：**计划已定（2026-08-14）**；待用户确认先做哪格后逐格实施（每格一个会话）。
> 目标平台：`d:\GitLab\EDA_EMG_ESP32`（ESP32-S3 + LVGL 9.5）
> 移植源：`d:\GitLab\EDA_EMG_LVGL_PC\PelFlo_ThePage\`（Child_MaiPage / Chlid_DysPage / Chlid_KgePage）
> 交叉引用：`0R_治疗页移植计划.md`（§7.3 治疗页结构 / §10 B5-C 完成记录 + 全部坑）、`0J`（通用移植指南）、开发计划「通用守则」
> 背景：B5-C 已移植四宫格 + 「治疗」格（9 子页）；四宫格保养/障碍/凯格尔三格当前为 `Show_StubPage` 占位。

---

## 1. 调研结论（2026-08-14，子代理全量对比，可直接执行无需重调研）

### 1.1 三格与治疗格结构对照
| 维度 | 治疗格（已移植参照） | 保养 MaiPage | 障碍 DysPage | 凯格尔 KgePage |
|---|---|---|---|---|
| 页数（枚举） | 9（Chart4 未实现） | 9（Chart4 未实现） | **5（无曲线页）** | 9（Chart4 未实现） |
| 阶段数 | 4 | 4 | **1**（15 次） | 4 |
| 曲线页 | Chart1/2/3 | 有（同治疗格） | **无** | 有（同治疗格） |
| Load 枚举 | `ChiThePage_*` | `ChiMaiPage_*` | `ChiDysPage_*` | `ChiKgePage_*` |

- 页面对应同构：Instr→xxPage1、StaSel→xxPage2、TreIns→xxPage3、ParSet→xxPage4、Treat→xxPage5、Chart1/2/3→xxPage6/7/8。
- 三格共用 `menu_ui.c` 的同一批共享控件（Instr/Select_Widget2/TreIns/ParSet/Treat/Chart_widget），**图标宏集合与治疗格完全一致 → SPIFFS 零新增**（`png/ThePage/` 29 bin + `page2/2_4_2.bin` 已全部存在）。

### 1.2 各格数据差异（与治疗格不同的字段；未列出的 = 与治疗格一致）
- **保养 MaiPage**：`Mai_Instr_data1` Title="盆底肌保养"、Text=产后养护文案；`Stage_Tbl`=[7,5,3,5]；`High_Obj`=700；Pulse=500；`Intens_Value`=5/6/7；MaiPage1_widget 容器用 `FONT_WHITE_COLOR,1`（治疗格是 GRAY,0，纯外观）。倒计时 5/3/1s 同。
- **障碍 DysPage**：Title="高张力盆底功能障碍治疗"、Text=高张力干预文案；`Stage_Tbl[1]`=15 次；`High_Obj`=500；`Stage_Num`=1；Pulse=600；`Intens_Value`=9；`remain_sec`=2s。**无曲线页**（DysPage1~5 只有 5 个 widget，Load switch 无 Chart 分支）。
- **凯格尔 KgePage**：Title="凯格尔训练"、Text=I/II 类肌纤维文案；`Stage_Tbl`=[12,12,12,12]；`High_Obj`=2500（48 格大滚动，`sta[48]` 刚好够，无需改结构体）；TreIns 标题特殊（data1="阶段一·治疗1"、data2="阶段二·初级训练"、data3="阶段三·中级训练"、data4="阶段四·高级训练"）；Pulse=400 同；Intens=1/2/3 同。

### 1.3 死代码清理（-Werror 必挂项）
| 格 | 需删 |
|---|---|
| 保养 | `#define MAX_TREAT_POINTS 20` + `static g_Treat_Points[20]` + `static g_Treat_Point_cnt`（未用） |
| 障碍 | `MAX_TREAT_POINTS 15` + `g_Treat_Points` + `g_Treat_Point_cnt`；**7 个非 static 死全局** `Dys_Instr_data/Dys_Select_data/Dys_Treins_data/Dys_Param_data/Dys_Treat_data/Dys_ctx/Dys_page`（不带 "1"，均未引用，且大结构体占 RAM） |
| 凯格尔 | `MAX_TREAT_POINTS 48` + `g_Treat_Points` + `g_Treat_Point_cnt`；**死数据 `Kge_Treins_data1_7`**（未进 `Kge_Treins_list`） |

### 1.4 共用事件 `PelFlo_TheChild_event.c` 恢复（B5-C 曾简化为 ThePage-only）
需补 3 个 include（Child_MaiPage.h / Chlid_DysPage.h / Chlid_KgePage.h）+ 恢复 7 处 switch：
| 回调 | ThePage | MaiPage | DysPage | KgePage |
|---|---|---|---|---|
| `timer_next_cb`（治疗→曲线1） | Chart1 | Chart1 | **无** | Chart1 |
| `Chart_Stage3_Add_data`（曲线1→2） | Chart2 | Chart2 | **无** | Chart2 |
| `Chart2_Stage3_Add_data`（曲线2→3） | Chart3 | Chart3 | **无** | Chart3 |
| `ChildPage_P4SetBtn_Event_cb`（→治疗） | Treat | Treat | Treat | Treat |
| `ChildPage_TreIns_Event_cb`（→参数） | ParSet | ParSet | ParSet | ParSet |
| `ChildPage_StageBtn_Event_cb`（→说明） | TreIns | TreIns | TreIns | TreIns |
| `Child_Page_StartBtn_Event_cb`（→疗程） | StaSel | StaSel | StaSel | StaSel |

- **前 3 处 PC 原文件本就无 DysPage 分支**（障碍无曲线页），保持无 Dys 即可。
- `Chart3_Stage3_Add_data`/`Chart_Stage2_4_Add_data`/`Chart_Btn_Event_cb` 已在 `menu_ui_event_cb.c`（只弹窗回菜单，不切页）→ **无需多格化**。

### 1.5 静态符号冲突
- 各格 `Stage_Tbl`、widget 函数、Load 函数、枚举、数据实例**前缀均不同 → 不冲突**。
- 各格需把治疗格 ESP32 版基础设施（`The_GroupRegister/s_the_objs/s_the_cnt/s_the_esc_obj/The_StopTimer/The_BackToMenu/ThePage_PageN_Key_cb/The_FindStage/The_StageUnSel/The_Page5_ShowConfirm`）复制并换成各自前缀（`Mai_*`/`Dys_*`/`Kge_*`）。
  - 障碍格**无曲线页** → 不需要 `s_the_esc_obj`/Chart 定时器停止，只做 P1~P5 Key 导航。
  - 保养/凯格尔需完整 9 页基础设施（StopTimer 遍历各自的 `MaiPage_CtxData[]`/`Mai_Chart_Data_List[]` 或 `KgePage_CtxData[]`/`Kge_Chart_Data_List[]`）。

### 1.6 GCC -Werror 修正
- `.Sta_Table = &Stage_Tbl`（指针到数组）→ `.Sta_Table = Stage_Tbl`（三格同治疗格坑 B5-C-3）。
- 倒计时 `%02d` 强转 `(int)`；LV_LOG `%d` 强转。

---

## 2. 执行步骤（每格一个子阶段，逐个可交付）

### 子阶段 B6-A：保养格（Child_MaiPage）
- [x] A1 新建 `main/ui_page_cn/PelFlo_ThePage/Child_MaiPage.c/h`（9 子页 + 数据实例 + `Mai_*` 基础设施）
- [x] A2 删死代码（MAX_TREAT_POINTS 20 族）+ `&Stage_Tbl→Stage_Tbl`
- [x] A3 `PelFlo_The_ui.c`：`Show_StubPage("盆底肌保养")` → `Child_MaiPage_ui()`（保留 LeaveGroup 清理）
- [x] A4 `PelFlo_TheChild_event.c`：补 include + 恢复 switch 的 MaiPage 分支（7 处中前 3 处加 Mai，后 4 处加 Mai）
- [x] A5 CMakeLists（已在 ui_page_cn/PelFlo_ThePage 目录内，无需改）→ 编译 + 烧录 + 目检

### 子阶段 B6-B：障碍格（Chlid_DysPage）
- [x] B1 新建 `main/ui_page_cn/PelFlo_ThePage/Chlid_DysPage.c/h`（5 子页无曲线 + `Dys_*` 基础设施）
- [x] B2 删死代码（MAX_TREAT_POINTS 15 族 + 7 个非 static 死全局）+ `&Stage_Tbl→Stage_Tbl`
- [x] B3 `PelFlo_The_ui.c`：`Show_StubPage("高张力盆底功能障碍")` → `Child_DysPage_ui()`
- [x] B4 `PelFlo_TheChild_event.c`：补 include + switch 加 DysPage 分支（仅后 4 处）
- [x] B5 编译 + 烧录 + 目检

### 子阶段 B6-C：凯格尔格（Chlid_KgePage）
- [x] C1 新建 `main/ui_page_cn/PelFlo_ThePage/Chlid_KgePage.c/h`（9 子页 + `Kge_*` 基础设施）
- [x] C2 删死代码（MAX_TREAT_POINTS 48 族 + `Kge_Treins_data1_7`）+ `&Stage_Tbl→Stage_Tbl`
- [x] C3 `PelFlo_The_ui.c`：`Show_StubPage("凯格尔训练")` → `Child_KgePage_ui()`
- [x] C4 `PelFlo_TheChild_event.c`：补 include + switch 加 KgePage 分支
- [x] C5 编译 + 烧录 + 目检（复测通过）

### 子阶段 B6-D：收尾
- [ ] D1 回归（治疗格/评估/设置/产后/菜单/英文）+ `check_spiffs_refs`（应 MISSING 0）
- [ ] D2 文档落库（本文件补完成记录 + 坑；开发计划阶段 B6 勾选；同步记忆）
- [ ] D3 本地 commit（**用户明确要求时才提交**；回家无法 push，回公司补推）

---

## 3. 相关文件
- 移植源（PC 只读）：`Child_MaiPage.c/h`、`Chlid_DysPage.c/h`、`Chlid_KgePage.c/h`、`PelFlo_TheChild_event.c`（PC 原 switch）
- 目标（ESP32）：新建 `main/ui_page_cn/PelFlo_ThePage/Child_MaiPage.c/h`、`Chlid_DysPage.c/h`、`Chlid_KgePage.c/h`；改 `PelFlo_The_ui.c`（3 处 stub）、`PelFlo_TheChild_event.c`（恢复 switch）
- 资源：`png/ThePage/`（29 bin）+ `page2/2_4_2.bin` —— **已全部存在，零新增**

## 4. 明确排除 / 遗留
- 英文三格（英文治疗页整体未做）
- 真实数据/控制（阶段 C/D）
- 历史按钮记录列表（阶段 D）
- 曲线图/倒计时纯演示值（同治疗格）

## 5. 待执行时确认
1. **先做哪格**（用户拍板）：推荐按复杂度 障碍(5页无曲线)→保养→凯格尔，或按四宫格布局 保养→障碍→凯格尔。
2. 凯格尔 TreIns 标题特殊性（阶段二~四为"初级/中级/高级训练"）目检时确认显示正确。
3. 凯格尔 High_Obj=2500 大滚动页按键导航体验（48 格 2D 导航）需目检确认。

---

## 6. B6-A 完成记录（2026-08-14，编译 app 0x181320 / 62% free + 烧录 + 目检通过）

### 6.1 改动清单
- 新建 `main/ui_page_cn/PelFlo_ThePage/Child_MaiPage.c/h`（保养格 9 子页 + 数据实例 + `Mai_*` 基础设施）
- `PelFlo_The_ui.c`：include Child_MaiPage.h + `PelFlo_ThePage_Load` 的 MaiPage case → `Child_MaiPage_ui()` + 切页清理加 `Child_MaiPage_LeaveGroup()`
- `PelFlo_TheChild_event.c`：补 include Child_MaiPage.h + 恢复 7 处多格 switch 的 MaiPage 分支（P5Btn Back 退出分派 The/Mai；timer_next/Chart_Stage3/Chart2_Stage3/P4Set/TreIns/StageBtn/StartBtn 加 Mai case；Dys/Kge 留后续会话）
- 数据差异：Title="盆底肌保养"、Stage_Tbl=[7,5,3,5]、High_Obj=700、Pulse=500、Intens=5/6/7、MaiPage1_widget 容器 WHITE,1（PC 原样）
- 删死代码：MAX_TREAT_POINTS 20 + g_Treat_Points + g_Treat_Point_cnt（-Werror unused 必挂）；`&Stage_Tbl→Stage_Tbl`（坑 B5-C-3 同款）

### 6.2 新坑 / 移植要点
- **坑 B6-A-1（关键，use-after-free）**：Page5 ESC → 确认弹窗 → 确认「是」→ `Back_Btn_Event_cb` → `Goto_MenuPage` **不删 timer** → 治疗 timer 继续访问已删对象 → 崩溃隐患。修复：ESC 分支（含 KEY/CLICKED）先 `lv_timer_del(ctx->timer_Treat)` 再 `The_Page5_ShowConfirm`；ESC 取消则治疗已暂停需重新开始。**保养格已修 + 治疗格同隐患一并修复**（用户拍板）。
- **坑 B6-A-2**：共用事件 P5Btn Back 分支（remain==0 直接回菜单）需按当前格分派退出——`Get_CurPage_Idx()` switch The→`The_BackToMenu()`/Mai→`Mai_BackToMenu()`（停对应格 timer + 移对应格 group），不能只调治疗格 `The_BackToMenu()`。
- `The_Page5_ShowConfirm`（治疗格非 static）被保养格复用——它只操作 ctx->Window + 全局 group，不依赖具体格，无需复制。

### 6.3 目检结果（✅ 2026-08-14 用户确认「通过，无问题」）
- 保养 BY 9 子页全链路 + 返回/ESC + 曲线图 + 确认弹窗 ✅
- 治疗格回归（共用事件 switch 恢复未破坏）✅
- 治疗格 ESC 隐患修复后复测待用户（本次已一并烧录）

### 6.4 遗留
- 障碍格 / 凯格尔格（各自后续会话）；英文三格；历史 stub；真实数据/控制（阶段 C/D）
- 提交（用户明确要求时才提交）

---

## 7. B6-B 完成记录（2026-08-17，编译 app 0x182280 / 62% free + 烧录 + 目检通过）

### 7.1 改动清单
- 新建 `main/ui_page_cn/PelFlo_ThePage/Chlid_DysPage.c/h`（障碍格 5 子页无曲线 + `Dys_*` 基础设施）
- `PelFlo_The_ui.c`：include Chlid_DysPage.h + `PelFlo_ThePage_Load` 的 DysPage case → `Child_DysPage_ui()` + 切页清理加 `Child_DysPage_LeaveGroup()`
- `PelFlo_TheChild_event.c`：补 include Chlid_DysPage.h + 后 4 处 switch 加 DysPage 分支（P5Btn Back 退出分派 The/Mai/Dys；P4Set/TreIns/StageBtn/StartBtn 加 Dys case；**前 3 处 timer_next/Chart_Stage3/Chart2_Stage3 无 Dys 分支**——障碍无曲线页，PC 原版即无）
- 数据差异：Title="高张力盆底功能障碍治疗"、`Stage_Tbl[1]`（阶段一 15 次）、`High_Obj=500`、`Stage_Num=1`、Pulse=600、`Intens=9`、`remain_sec=2s`、`Child_DysPage_Load` 各 case 直传（无 list/数组）
- 删死代码：**7 个非 static 死全局**（Dys_Instr_data/Dys_Select_data/Dys_Treins_data/Dys_Param_data/Dys_Treat_data/Dys_ctx/Dys_page，均未引用且大结构体占 RAM）+ MAX_TREAT_POINTS 15 族；`&Stage_Tbl→Stage_Tbl`

### 7.2 新坑 / 移植要点
- **障碍格无曲线页** → 基础设施简化：无 `s_esc_obj`/无 Chart 定时器停止；`Dys_StopTimer` 只处理单例 `DysPage_CtxData1`（无数组）；Page5 ESC 同款坑 B6-A-1 修复（先删 timer 再确认）
- `Child_DysPage_Load` 的 `uint8_t idx = Get_CurStage_Idx()`（PC 未用）→ 删（unused 告警）
- 倒计时结束：障碍格仅阶段一 → `Trest_Widget_timer_cb` 走 STAGE_1 分支「很棒哦~」→ 无 timer_next → 无曲线（行为正确）

### 7.3 目检结果（✅ 2026-08-17 用户确认「通过，无问题」）
- 障碍 ZA 5 子页全链路（说明→15 格疗程→阶段说明→参数(600uA)→治疗倒计时(强度9)）+ 返回/ESC + 确认弹窗 ✅
- 保养格回归（共用事件 switch 多格化未破坏）✅

### 7.4 遗留
- 凯格尔格（下一会话）；英文三格；历史 stub；真实数据/控制（阶段 C/D）
- 提交（用户明确要求时才提交）

---

## 8. B6-C 完成记录（2026-08-17，编译 app 0xc86c0 / 80% free + 烧录 + 复测通过）

### 8.1 改动清单
- 新建 `main/ui_page_cn/PelFlo_ThePage/Chlid_KgePage.c/h`（凯格尔格 9 子页含曲线 + `Kge_*` 基础设施，KGE_GROUP_MAX=64）
- `PelFlo_The_ui.c`：include Chlid_KgePage.h + `PelFlo_ThePage_Load` 的 KgePage case → `Child_KgePage_ui()` + 切页清理加 `Child_KgePage_LeaveGroup()`
- `PelFlo_TheChild_event.c`：补 include Chlid_KgePage.h + **全部 7 处** switch 加 KgePage 分支（P5Btn Back 分派 The/Mai/Dys/Kge；前 3 处 timer_next/Chart_Stage3/Chart2_Stage3 + 后 4 处 P4Set/TreIns/StageBtn/StartBtn）
- 数据差异：Title="凯格尔训练"、`Stage_Tbl`=[12,12,12,12]（48 格）、`High_Obj=2500`（大滚动）、TreIns 标题特殊（阶段二/三/四="初级/中级/高级训练"）、Pulse=400、Intens=1/2/3
- 删死代码：`Kge_Treins_data1_7`（死数据未进 list）+ MAX_TREAT_POINTS 48 族；`&Stage_Tbl→Stage_Tbl`

### 8.2 新坑 / 移植要点
- **坑 B6-C-1（编译，括号）**：B6-C 替换共用事件 switch 时 `ChildPage_StageBtn_Event_cb` 的 `if (num < Btn_MaxSum)` 块缺一个闭合 `}` → 后续所有 static 函数（timer_next_cb/Get_StageBtn/Set_StageBtn_UnSel）被嵌进函数体 → `invalid storage class` 一堆错误。修复：补 `        }`。**教训：多格 switch 化时用 multi_replace 逐处替换，易因 oldString 边界（尾随空格/是否含外层 `}`）破坏括号，改完必须整文件过一遍括号配平。**
- **坑 B6-C-2（目检发现，四宫格 ESC 卡住）**：治疗页一直 ESC 回退到四宫格后再按返回 → **卡住（无崩溃，心跳正常）**。根因：四宫格 `ThePage_MenuBtn_event_cb` KEY 分支只处理 2D 导航，**漏了 ESC 回主菜单**。修复：`The_LeaveGroup` 改非 static（声明到 PelFlo_The_ui.h），KEY 分支加 `if (key == LV_KEY_ESC) { The_LeaveGroup(); Goto_MenuPage(); return; }`（坑 6.8 先移 group + 坑 B4-3 提前 return）。
- **字库 GC（预期优化）**：三格全做完后 `Show_StubPage` 无调用者 → 链接器 `--gc-sections` 裁掉它 + `lv_font_simhei_24px` 全量 GB2312 字库（~700KB）→ **固件从 0x182280(62% free) 缩到 0xc86c0(80% free)**。真页用 SPIFFS 的 .bin 字体不受影响。后续可考虑从 CMake SRC_DIRS 移除 `ui/font/`（省编译时间）或删死代码。
- 凯格尔 48 格大滚动 + 2D 导航实测正常（3 格/行 index 运算）。

### 8.3 目检结果（2026-08-17）
- 首检发现四宫格 ESC 卡住（坑 B6-C-2）→ 修复烧录 → **用户复测通过** ✅
- 凯格尔 9 子页全链路（48 格网格/阶段说明/参数/治疗/曲线 1-2-3）+ 返回/ESC + 保养/障碍/治疗格回归 ✅

### 8.4 遗留
- 英文三格；历史 stub；真实数据/控制（阶段 C/D）
- 可选清理：`Show_StubPage` + `ui/font/`（simhei 字库）已无调用者，可删死代码/移出编译（省编译时间）
- 提交（用户明确要求时才提交）
